#Password file used for building the mosquitto image.
#Once built and started, you need to log on to the mosquitto image
#and encrypt the password using the command 'mosquitto_passwd -c /mosquitto/config/passwd.txt'
#

button:pressme!!
